&НаКлиенте
Процедура ПоставитьОтметкуОбОплате(Команда)
	
	Если Объект.Ссылка.Пустая() Тогда
		Сообщить("Сначала запишите документ!");
		Возврат;
	КонецЕсли;
	

	ПоставитьОтметкуНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПоставитьОтметкуНаСервере()
	
	ДокОбъект = Объект.Ссылка.ПолучитьОбъект();
	
	Если ДокОбъект <> Неопределено Тогда
		
		ДокОбъект.Оплачено = Истина;
		
		
		ДокОбъект.Записать();
		
		
		ЗначениеВРеквизитФормы(ДокОбъект, "Объект");
		
		Сообщить("Документ оплачен");
	КонецЕсли;
КонецПроцедуры

