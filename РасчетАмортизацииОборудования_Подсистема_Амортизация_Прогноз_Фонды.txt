&НаКлиенте
Процедура Сформировать(Команда)
	СформироватьНаСервере();
КонецПроцедуры

&НаСервере
Процедура СформироватьНаСервере()
	
		Результат.Очистить();
	
	ТабДок = Результат;
	
		Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Логи.Аппарат КАК Оборудование,
		|	Логи.Аппарат.Наименование КАК НаименованиеАппарата,
		|	СУММА(Логи.Количество) КАК КоличествоЦикловИлиКг,
		|	ВЫБОР
		|				|		КОГДА Логи.Аппарат.Наименование ПОДОБНО ""%ТПА%"" 
		|		   ИЛИ Логи.Аппарат.Наименование ПОДОБНО ""%Jetmaster%"" 
		|		   ИЛИ Логи.Аппарат.Наименование ПОДОБНО ""%Аппаратдлялитья%"" 
		|		   ИЛИ Логи.Аппарат.Наименование ПОДОБНО ""%Лить%"" ТОГДА СУММА(Логи.Количество) * 0.17
		|		
		|				|		КОГДА Логи.Аппарат.Наименование ПОДОБНО ""%Дробилк%"" ТОГДА СУММА(Логи.Количество) * 0.0454
		|		
		|			|		КОГДА Логи.Аппарат.Наименование ПОДОБНО ""%Сушилк%"" 
		|		   ИЛИ Логи.Аппарат.Наименование ПОДОБНО ""%SHINI%"" ТОГДА СУММА(Логи.Количество) * 0.01
		|		
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК СуммаАмортизации,
		|	
		|			|	ВЫБОР
		|		КОГДА Логи.Аппарат.Наименование ПОДОБНО ""%ТПА%"" 
		|		   ИЛИ Логи.Аппарат.Наименование ПОДОБНО ""%Jetmaster%"" 
		|		   ИЛИ Логи.Аппарат.Наименование ПОДОБНО ""%Аппаратдлялитья%"" 
		|		   ИЛИ Логи.Аппарат.Наименование ПОДОБНО ""%Лить%"" ТОГДА ИСТИНА
		|		ИНАЧЕ ЛОЖЬ
		|	КОНЕЦ КАК ЭтоЛитьевойАппарат
		|ИЗ
		|	Документ.ПроизводственныеЛоги КАК Логи
		|ГДЕ
		|	Логи.Проведен
		|	И Логи.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания
		|
		|СГРУППИРОВАТЬ ПО
		|	Логи.Аппарат,
		|	Логи.Аппарат.Наименование";
		
		НачДня = ?(ЗначениеЗаполнено(ДатаНачала), НачалоДня(ДатаНачала), '20000101000000');
	КонДня = ?(ЗначениеЗаполнено(ДатаОкончания), КонецДня(ДатаОкончания), КонецДня(ТекущаяДата()));

	Запрос.УстановитьПараметр("ДатаНачала", НачДня);
	Запрос.УстановитьПараметр("ДатаОкончания", КонДня);
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
		ОбластьШапка = Новый ТабличныйДокумент;
	ОбластьШапка.Область("R1C1").Текст = "Аппараты";
	ОбластьШапка.Область("R1C2").Текст = "циклов/ кг";
	ОбластьШапка.Область("R1C3").Текст = "Сумма₴";
	ОбластьШапка.Область("R1C1:R1C3").Шрифт = Новый Шрифт(,, Истина);
	ТабДок.Вывести(ОбластьШапка);
	
		ИтогоАмортизация = 0;
	
		Пока Выборка.Следующий() Цикл
		
				ЧислоЦикловКг = Окр(Выборка.КоличествоЦикловИлиКг, 2);
		СуммаАморт = Окр(Выборка.СуммаАмортизации, 2);
		
				ОбластьСтрока = Новый ТабличныйДокумент;
		ОбластьСтрока.Область("R1C1").Текст = Выборка.НаименованиеАппарата;
		ОбластьСтрока.Область("R1C2").Текст = ЧислоЦикловКг;
		ОбластьСтрока.Область("R1C3").Текст = СуммаАморт;
		ТабДок.Вывести(ОбластьСтрока);
		
		ИтогоАмортизация = ИтогоАмортизация + СуммаАморт;
		
			Если Выборка.ЭтоЛитьевойАппарат Тогда
			СуммаПрессФормы = Окр(Выборка.КоличествоЦикловИлиКг * 0.195, 2);
			
			ОбластьПрессФорма = Новый ТабличныйДокумент;
			ОбластьПрессФорма.Область("R1C1").Текст = "Пресс-форма";
			ОбластьПрессФорма.Область("R1C2").Текст = ЧислоЦикловКг;
			ОбластьПрессФорма.Область("R1C3").Текст = СуммаПрессФормы;
			ТабДок.Вывести(ОбластьПрессФорма);
			
			ИтогоАмортизация = ИтогоАмортизация + СуммаПрессФормы;
		КонецЕсли;
		
	КонецЦикла;
	
		ИтогоАмортизация = Окр(ИтогоАмортизация, 2);
	
		ОбластьИтого = Новый ТабличныйДокумент;
	ОбластьИтого.Область("R1C1").Текст = "ИТОГО:";
	ОбластьИтого.Область("R1C2").Текст = "";
	ОбластьИтого.Область("R1C3").Текст = ИтогоАмортизация;
	ОбластьИтого.Область("R1C1:R1C3").Шрифт = Новый Шрифт(,, Истина);
	ТабДок.Вывести(ОбластьИтого);
		
КонецПроцедуры


